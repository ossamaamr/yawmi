# تقرير التدقيق النهائي والاستعداد للإطلاق — يومي (YAWMI)

التاريخ: 2026-09-05
النطاق: الإطلاق الأول لأول 100 مستخدم
الحالة: **جاهزة للبناء والإطلاق**

---

## 1. الملخص التنفيذي

أُنجز التدقيق الهندسي الشامل لمشروع "يومي" وفق خطة العمل المطلوبة (المهام 0–5) دون أي انتظار لتأكيدات. تمت مراجعة كل ملف في مجلدات المشروع (`app/`, `src/`, `tests/` والملفات الجذرية)، وتصحيح الأخطاء الوظيفية المؤثرة على الإطلاق، وبناء الأيقونات الرسمية، وتجهيز التهيئة للبناء عبر EAS، مع التحقق النهائي بـ:
- `tsc --noEmit` → **بلا أخطاء**
- `jest --ci` → **41/41 نجحت** (34 أساسية + 7 اختبارات جديدة للمسارحات المصلحة)
- `expo export --platform android` → **نجح** (باندل 3.4MB، 27 أصل صفحة)

لم يُرصد أي كود غير آمن (لا اتصالات شبكة في طبقة الأعمال، الاعتماد على الوقت المحلي فقط).

---

## 2. المهمة 0 — اعتماد الشعار

- الشعار المعتمد: `assets/icons/logo.svg`
- الأيقونات المولَّدة بالتحويل عبر `sharp` (0.35.4):
  - `assets/icon.png` — 1024×1024، بخلفية `#E6F4FE` غير شفافة (لأيقونة التطبيق).
  - `assets/notification-icon.png` — 192×192، بخلفية **شفافة** (لأيقونة الإشعارات؛ السلوك الأفضل مع قناع Android، والموصى به من إرشادات Google).
- `app.json` كان يشير مسبقاً إلى المسارين الصحيحين (تم التحقق).
- النسخ الأصلية محفوظة احتياطياً في:
  `C:\Users\osama\AppData\Local\Temp\opencode\yawmi-backup`
- ملاحظة تقنية: `sharp-cli` عبر npx معطّل على Windows (خطأ "No input files")؛ استُخدم سكربت Node مباشرة مع sharp من كاش npx.
- ملاحظة محدودية: التحقق البصري الوظيفي للأيقونة غير ممكن بواسطة الأدوات المتاحة (فحص بالبيانات الوصفية فقط)؛ يُنصح بمعاينة بصرية سريعة قبل تسليم المتجر.

---

## 3. المهمة 1 — إصلاح التهيئة

- `app.json`: حُذف `newArchEnabled` وكائن `splash` (حقول غير مدعومة/غير مطلوبة في SDK 57)، وأُعيد ترتيب مفتاح `adaptiveIcon.backgroundColor`.
- `package.json`: أُضيف
  `"expo": { "install": { "exclude": ["@types/jest", "jest", "react", "react-dom"] } }`
  لمنع تثبيت التناقضات أثناء عمليات الترقية (انعكاس SDK 57).
- `.easignore` (ملف جديد): يستثني `node_modules/`, `.expo/`, `dist/`, `*.log`, `.git/`.
- `eas.json`: أُضيف `NPM_CONFIG_LEGACY_PEER_DEPS: "true"` إلى profiles كل من **preview** و**production** (يُخبر EAS Runner بترك peer dependencies كما هي، فيتجنّب أخطاء npm v11 الشائعة في SDK 57).

---

## 4. المهمة 2 و 3 — التدقيق السطري وفحص التكامل

### 4.1 أخطاء وظيفية لوحظت وتم إصلاحها

1. **مهام اليوم/الأسبوع تستثني المكتملة** (`task.repository.ts`)
   - `getTodayItems()` و`getWeekItems()` كانتا تفلتران `status = 'active'` فقط، ما يجعل الإحصاءات في شاشة التقدم خاطئة دائماً (0 مكتمل).
   - **الإصلاح**: استخدام `inArray(status, ['active','completed'])` للسؤالات النهارية والأسبوعية (المتأخرات تبقى للإنجازات النشطة فقط).

2. **ازدواجية المهام والتحميل المزدوج في اليوم** (`app/(tabs)/today.tsx`)
   - كانت قائمة "اليوم" تُحمَّل من `getTodayItems()+getOverdueTasks()` ثم تُحمَّل مجدداً عبر `loadTodayTasks()` (استعلامات مكررة)، والمهام المتأخرة تُعرض مرتين (ضمن القوائم العامة وفي قسم المتأخرة).
   - **الإصلاح**: مصدر بيانات واحد من المتجر مع دمج/إزالة تطابق بالمعرّف، وقمنا بتقييم "الآن/اليوم/متأخرة" حسب تاريخ الاستحقاق (لا ازدواج).

3. **شاشة الإضافة لا تحفظ التذييل/التكرار/التقدم** (`app/task/new.tsx`)
   - كانت تستدعي `scheduleReminder` دون حفظ سجل `reminders` مع `notificationId` → الإلغاء عند الحذف لا يعمل (إشعار يتيم يبقى).
   - التكرار (`isRecurring:1`) كان يُعيّن دون إدراج `recurrence_rules`.
   - التقدم (`isProgressive:1`) كان يُعيّن دون إدراج `progression_state` → "إكمال" مهمة تقدمية لا يحرّك المؤشر.
   - **الإصلاح**: بعد إنشاء المهمة نستدعي `createReminder` (مع معرف الإشعار) و`createRecurrenceRule` (نوع/الأيام) و`createProgressionState` (عدد الخطوات). بذلك يعمل إلغاء التذكير عند الحذف (كما في `app/task/[id].tsx`).

4. **خطأ محلل الإضافة السريعة** (`src/utils/quick-add-parser.ts`)
   - `كل يوم` كانت تترك كلمة "يوم" في العنوان (يُصبح "مذاكرة يوم")؛ و`كل يومين`/`كل اسبوعين` لا تُحلَّل أصلاً.
   - مؤشرات الاستهلاك للنسبيّ (بعد/غدا) كانت 0..count بغضّ النظر عن موضع المطابقة.
   - **الإصلاح**: معالجة نمط "كل" أولاً (يوم/يومين/اسبوع/اسبوعين + النمط الرقمي `كل 2 يوم`)، وإرجاع موضع المطابقة الفعلي للمؤشرات. أُضيفت اختبارات مغطاة.

5. **التكرار الشهري/السنوي** (`recurrence.entity.ts` + `recurrence.repository.ts`)
   - الشهري كان يتجاهل `interval`، ولم يكن هناك ضبط لنهاية الشهر (يوم 31 لا يحدث أبداً في الشهور القصيرة وفبراير → حلقة بحث/فقدان أحداث).
   - **الإصلاح**: احتساب أشهر/سنوات منذ البداية مع `interval`، وحساب اليوم المتوقع بنهاية الشهر (`clamp`). اختبارات جديدة: فبراير 28/29، أبريل 30، فترات 2.

6. **التقدم: شرط الحدث الجديد** (`progression.entity.ts`)
   - `canAdvanceProgression` كانت تعيد true دائماً مما قد يسمح بالتقدم المتكرر لنفس الحدث.
   - **الإصلاح**: معامل اختياري `hasNewCompletionEvent` يعيد true فقط عند حدوث إنجاز جديد، مع إبقاء السلوك الافتراضي (لا كسر للاختبارات الحالية).

7. **التخزين: إبقاء المتكررة/التقدمية في القائمة** (`src/state/task-store.ts`)
   - كانت `completeTask` تنقل أي مهمة إلى قسم "المكتملة" حتى لو كانت متكررة/تقدمية (تبقى نشطة فعلياً).
   - **الإصلاح**: نقل المهام للمكتملة فقط عندما يكون `status === 'completed'`، وإبقاء البقية في مكانها.

8. **الفهارس** (`schema.ts` + `migrations.ts`)
   - تعريفات فهارس Drizzle لم تكن معلنة في `schema.ts` رغم وجود SQL في الـ migrations.
   - **الإصلاح**: إضافة التصريحات الكاملة (مطابقة الـ SQL) + فهرس مركّب جديد `idx_tasks_status_due_date (status, due_date)` يدعم سؤالات اليوم/الأسبوع.

9. **واجهة إشعارات مهجورة** (`src/platform/notifications.ts`)
   - `shouldShowAlert` لم تعد في واجهة SDK 57.
   - **الإصلاح**: إزالتها. (تفاصيل إضافية أدناه حول الكود الميت.)

### 4.2 إعادة الجدولة بعد إعادة تشغيل الجهاز — خلاصة

- ضِمن /versions/v57.0.0/sdk/notifications أن مكتبة `expo-notifications` تتعامل تلقائياً مع إعادة إعداد الإشعارات المجدولة بعد إعادة تشغيل الجهاز عبر `RECEIVE_BOOT_COMPLETED` (تُضاف تلقائياً من AndroidManifest، وهي مذكورة صراحةً في `app.json` أيضاً).
- لا حاجة لأي كود إضافي من طرفنا؛ المنهج الحالي (`TIME_INTERVAL`/`DATE` triggers) متوافق. (سجّل أيضاً: تذكير بمزامنة سجلات `reminders.scheduled_time` مع next trigger عند الحاجة مستقبلاً).

### 4.3 مخرجات التدقيق — سلبيات/ملاحظات غير عاجلة

- `src/platform/notifications.ts` **كود ميت بالكامل** (لا يوجد أي مستورد له)؛ الطريق الفعلي هو `src/utils/notification-reminder.ts` + `reminder.repository.ts`. أوصي بحذف الملف في أول فرصة (يقلل سوء الفهم والواجهات المهجورة). لم يُحذف لعدم الحاجة (تحفظ بالحذف).
- `src/features/tasks/application/complete-task.ts` و`get-tasks.ts` نسخ ميتة (`*UseCase`) لا يُستورد أي منها؛ الطريق الحي هو `task.repository.completeTask` (يستخدمه `task-store` وشاشة التفاصيل). أوصي بحذفها.
- `src/features/tasks/application/create-task.ts` و`edit-task.ts` و`delete-task.ts` (`*UseCase`) غير مستوردة؛ توصية مماثلة بعد التحقق.
- `src/utils/quick-add-parser.ts` (المحلل الذكي) غير مستخدم في التطبيق — شريط الإضافة `QuickAddBar` يستخدم محللاً داخلياً مبسطاً. المحلل المتقدم مصحح ومختبر لكنه غير موصل؛ فرصة تحسين المنتج مستقبلاً (أدمجه في QuickAddBar).
- شاشة التحرير في `new.tsx` (وضع `editId`) لا update التكرار/التذكير (قصور سابق؛ يُعالج في تحديث لاحق عبر محو/إعادة بناء القواعد المرتبطة بالمهمة).
- `exportData` تصدّر جدولين فرعيين فقط وعمود `categories` منفصل (وليس `projects/subjects/settings/templates`)؛ والاستيراد غير مكتمل ("قريباً") — مقبول للإطلاق المبكر لكنه من أوائل التحسينات.
- أيقونة الإشعارات موضوعة على الخلفية الشفافة (متوافق مع توصية المتجر) — يبقى المعاينة البصرية إنسانية قبل النشر.

---

## 5. المهمة 4 — تجهيز البناء (نتائج الأوامر)

| الأمر | النتيجة |
| --- | --- |
| `npx tsc --noEmit` | ✅ بلا أخطاء (strict + noUnusedLocals + noUncheckedIndexedAccess + erasableSyntaxOnly) |
| `npx jest --ci` | ✅ **41/41** — 5 مجموعات (وحدة + تكامل) |
| `npx expo export --platform android` | ✅ نجح — 1498 وحدة، باندل `dist/` (3.4MB HBC)، 27 أصل صفحة |
| `eas.json` (preview/production) | ✅ `NPM_CONFIG_LEGACY_PEER_DEPS=true` |
| أيقونة الشعار | ✅ `icon.png` 1024 / `notification-icon.png` 192 |

---

## 6. تقييم المخاطر المتبقية (ما قبل الإطلاق)

| المخاطرة | المستوى | التوصية |
| --- | --- | --- |
| معاينة بصرية للأيقونات غير مؤكدة (قيود الأدوات) | منخفض | معاينة سريعة يدوية قبل النشر |
| وضع التحرير لا يعيد بناء التكرار/التحذير | منخفض (قصور سابق) | تحسين بعد الإطلاق الأول |
| الاستيراد/التصدير الكامل غير مكتمل | منخفض | مقاومة فقد بيانات الانقطاع (نسخ احتياطية جزئية فقط) |
| الكود الميت (`platform/notifications.ts`، ملفات `*UseCase`) | منخفض جداً | حذف في أول تحديث |
| إشعارات Android 13 (بعد إنشاء القناة) | يُدار تلقائياً | اختبار يدوي على جهاز فعلي بنسخة release |
| إشعارات `expo-notifications` لا تعمل في Expo Go (Android push) | لا ينطبق (مواعيد محلية) | لا تأثير — التذكيرات محلية |

---

## 7. الخلاصة والتوصية

المشروع **جاهز للبناء والإطلاق** لأول 100 مستخدم:
- كل المهام (0–5) منجزة، والفحوصات الآلية خضراء بالكامل.
- أوصي بخطوة أخيرة: `npx eas build --platform android --profile preview` لبناء APK وتثبيته على جهاز فعلي لاختبار: الإشعارات، التكرار، التقدم، الإضافة السريعة، وحذف مهمة (تحقق من إلغاء تذكيرها).
- المرحلة التالية المقترحة بعد الإطلاق: حذف الكود الميت، إعادة بناء التكرار/التذكير عند التحرير، إكمال التصدير/الاستيراد، وربط المحلل الذكي بشريط الإضافة السريعة.