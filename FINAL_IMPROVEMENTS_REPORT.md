# التقرير النهائي — تحديث يومي الشامل

**المشروع:** يومي (Yawmi) — Capacitor + Vite + React + IndexedDB
**التاريخ:** 6 سبتمبر 2026
**الفرع:** `main` — آخر commit: `ad1739a`

---

## ملخص التنفيذ

نُفِّذت المهام الأربع عشرة كاملة. التطبيق الآن يتضمن جوهره الجديد — **الورد القرآني اليومي** — مع شريط حماسة، صفحة للمهام المتكررة، نغمة منبه من الجهاز، إصلاح شامل للوقت والإشعارات، وواجهة عربية 100% بلا أي كلمة إنجليزية أو تقنية ظاهرة.

## المهام الأولى إلى الرابعة عشر

### أولاً: إصلاح أسماء الأسبوع ✔
- `src/pages/Week.jsx` أعيدت كتابتها صب `DAY_LABELS = ['الأحد','الإثنين','الثلاثاء','الأربعاء','الخميس','الجمعة','السبت']` (إزالة إضافة أيام الترجمة الناقصة، بل أسماء كاملة في شريط الأسبوع). نُقلت الأسماء الكاملة أيضًا إلى مخطط الأعمدة في `Progress.jsx`.

### ثانياً: إصلاح الإشعارات (الأذونات) ✔
- في `src/lib/notifications.js`: دالة `requestNotificationPermission()` تستخدم `LocalNotifications.requestPermissions()` وتقبل نتائج `granted` و`limited`.
- دالة جديدة `openSystemNotificationSettings()` تعيد التوجيه إلى إعدادات النظام عند رفض الإذن.
- الإشعارات تُجدول وتُحذف عند حذف المهمة (عبر `cancel` قبل الحذف).

### ثالثاً: جوهر التطبيق — الورد اليومي من القرآن ✔
- **`src/lib/quran.js`**: محرك كامل يتضمن
  - جداول 30 جزءًا و114 سورة (الاسم، صفحة البداية، عدد الآيات) مطابقة للمصحف المدمحاني (604 صفحات).
  - أنواع الورد: جزء يومياً / حزب يومياً / ربع يومياً / صفحة واحدة / 5 صفحات / ختمة في 30 يوماً.
  - دوال: نطاق صفحات اليوم، السورة الحالية، المقاطع (سورة + الآيات + الصفحة)، نسبة الإنجاز، حساب الحماسة.
  - يُخزَّن في `settings` (IndexedDB): نوع الورد، اليوم الحالي، الأيام المنجزة، الإجمالي، الحماسة، آخر يوم اكتمل.
- **`src/pages/QuranDaily.jsx`**: صفحة الورد بقائمة منسدلة، شريط تقدم، عرض المهمة اليومية (الجزء/السورة/الآيات/الصفحة)، زر "أكملت هذا الورد اليوم" الذي يقدم اليوم التالي تلقائياً.
- **ملاحظة (موثقة):** توزيع أرقام الآيات على الصفحات تقديري مبسط (تناسب خطي عبر نطاق صفحات السورة) — كافٍ لواجهة الورد اليومي.

### رابعاً: إصلاح الوقت في إضافة المهمة ✔
- `src/pages/NewTask.jsx`: دالة `normalizeTime()` تحوّل الأرقام العربية إلى لاتينية وتنتج صيغة `"HH:MM"` ثابتة مع ضبط الحدود (23:59)، تعمل مع `input type="time"` على الويب وأندرويد.
- الوقت يصل إلى `scheduleNotification` لتُجدول الإشعارات بدقة، ويُعرض في البطاقات بصيغة عربية (مثل "٧:٣٠ صباحاً").

### خامساً: الوقت الحالي والتحية في الأعلى ✔
- **`src/components/Header.jsx`**: تحية حسب الوقت المحلي (صباح الخير/طاب يومك/مساء الخير/طابت ليلتك)، ساعة حية تتحدث كل 30 ثانية بصيغة "٧:٣٠ صباحاً"، والتاريخ الكامل (الهجري + الميلادي) من ساعة الجهاز.
- أُضيف إلى `Layout.jsx` أعلى الصفحة قبل `Outlet`.

### سادساً: الحماسة المستمرة (Streak) ✔
- تُحسب في `completeQuranWird` وتُخزَّن تحت `quranStreak`؛ تزيد يومياً وتعود إلى 1 بعد يوم فائت.
- ظاهرة في مكانين: بطاقة في أعلى صفحة اليوم (تنقل لصفحة الورد)، وسطر مخصص في صفحة الورد القرآني.

### سابعاً: صفحة المهام المتكررة ✔
- **`src/pages/RecurringTasks.jsx`**: تعرض المهام المتكررة؛ عند النقر تتوسع لتعرض تاريخ البدء، الأيام الفائتة (عبر `getOccurrencesInRange` + أحداث الإكمال)، وزر "الاستمرار من اليوم" (يعدّل تاريخ البدء)، وزر "تعديل" ينقل لنموذج المهمة.
- المسار `/recurring` أُضيف في `App.jsx` وزر التنقل في `Layout.jsx`.

### ثامناً: تحسين التباين والألوان ✔
- `src/index.css` أُعيدت كتابتها كاملة: تباين أعلى للنصوص الناعمة، ظلال خفيفة وحواف ناعمة، أزرار أكبر لللمس، دعم كامل للوضع الداكن لكل العناصر، وألوان مميزة للورد القرآني (أخضر داكن `#1d5e3e` في الفاتح / سهل في الداكن).

### تاسعاً: حذف أي كلمة إنجليزية أو تقنية ✔
- جميع أسماء الأنماط في كل الملفات ترجمت وفق الجدول المطلوب (`grow→مرن`, `stack→عمود`, `row→صف`, `row-between→صف-بين`, `card→بطاقة`, `btn→زر`, `input→مدخل`, `select→قائمة`, `field→حقل`, `empty→فارغ`, `spinner→مؤشر`, `badge→وسم`, `chip→زر-صغير` ...) مع أسماء جديدة للعناصر الجديدة (الشريط-العلوي، بطاقة-الورد، وسم-الحماسة...).
- تمت المراجعة النهائية بـ `eslint` و`grep`: لا توجد كلمة إنجليزية ظاهرة في واجهة المستخدم.

### عاشراً: اختيار نغمة منبه ✔
- زر "اختر نغمة منبه من جهازك" في نموذج المهمة يفتح منتقي الملفات الصوتية عبر `@capawesome/capacitor-file-picker` (مع fallback لمدخل ملف مخفي على الويب).
- المسار يُحفظ في حقل `ringtone` ويُمرَّر إلى `scheduleNotification({ sound })`.
- **ملاحظة (موثقة):** على أندرويد يقبل `LocalNotifications` أسماء موارد داخل التطبيق فقط؛ مسار الملف الخارجي يُمرَّر بأفضل جهد، ويجب أن تبقى نغمة افتراضية عند تعذر المعالجة. بديل مقترح مستقبلاً: دمج ملف صوتي داخل `res/raw` وتعيين اسمه.

### حادي عشر: الشعار وصورة التطبيق ✔ (موثق)
- الملفات موجودة: `assets/icon.png` (1024×1024)، `assets/notification-icon.png` (192×192)، `assets/splash-icon.png`، مع المتغيرات الصادرة من المولد.
- `capacitor.config.json` يشير إلى `assets/icon.png` و`assets/splash-icon.png`، و`LocalNotifications.smallIcon = ic_stat_icon`.
- الأيقونات تُولَّد داخل الـ CI عبر `capacitor-assets` (خطوة في workflow).

### الثاني عشر: تحديث التنقل ✔
- شريط سفلي من 6 عناصر مرتبة منطقياً: اليوم، الأسبوع، التقدم، الورد القرآني 📖، المتكررة 🔁، الإعدادات — مع إبراز العنصر النشط وتأثير حركي بسيط.

### الثالث عشر: تحسين شامل للصفحات الحالية ✔
- رُوجعت Today / Week / Progress / Settings: دعم RTL كامل، لا مصطلحات إنجليزية، توافق مع نظام الألوان الجديد، ظهور الوقت والتحية في الأعلى، وظهور الحماسة في Today و QuranDaily.

### الرابع عشر: الاختبار والرفع ✔
- `npm run lint` → **صفر أخطاء** (تم إضافة `dist` و`android` إلى `ignorePatterns` لأنها نواتج بناء).
- `npm run build` → **نجاح**.
- الرفع: commit `ad1739a` دُفع إلى `main`، عَمَلية Actions `34033218558` → **ناجحة**.
- **حجم الـ APK النهائي: 2.87 ميجابايت** (أقل بكثير من حد 30 ميجابايت).

---

## مخرجات الأوامر

| الأمر | النتيجة |
|---|---|
| `npm run lint` | صفر أخطاء، صفر تحذيرات |
| `npm run build` | نجح — 70 وحدة، dist جاهزة |
| `npx cap sync android` | 4 إضافات مزامنة |
| CI Build (push) | ✓ في 2د13ث |
| حجم `app-release.apk` | 2.87 MB ✓ |

## الاعتماديات المضافة

```
@capawesome/capacitor-file-picker@^6.2.0   (متوافقة مع Capacitor 6)
capacitor-native-settings@^6.0.6
```

## ملاحظات مهمة

1. **استبدال الحزمة (هام):** طلبت مواصفة `@capacitor/android-settings`، لكن التحقق من npm أظهر أنها **غير موجودة** (404). استُبدلت بـ `capacitor-native-settings@6.0.6` التي توفر نفس الهدف عبر `NativeSettings.open({ optionAndroid: AndroidSettings.AppNotification })`.
2. حد الـ Node 20 الموثوق في الـ CI (إشعار deprecation من GitHub Actions) لا يؤثر على النتائج الحالية؛ يُوصى بالترقية عند التعديل القادم.
3. محرك القرآن تقديري لرقم الآية داخل السورة (انظر ملاحظة المهمة الثالثة).
4. جميع التواريخ والأوقات من جهاز المستخدم (`Date.now()`) — لا خادم ولا مزامنة، التطبيق محلي بالكامل.
5. تذييل المطور "تطوير: أسامة بن عمرو السَّروجيّ" في أسفل جميع الصفحات.